using UnityEngine;
using System.Collections;

//Editorで表示させる用

public class EditorTiles : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
		Destroy (gameObject);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
